# A. Префикс-функция

Дана непустая строка $S$, длина которой $N$ не превышает $10^6$. Будем считать, что элементы строки нумеруются от $0$ до $N-1$.

Требуется для всех $i$ от $0$ до $N-1$ вычислить её префикс-функцию $\pi[i]$.

## Формат ввода

Одна строка длины $N$, $0 < N \le 10^6$, состоящая из маленьких латинских букв.

## Формат вывода

Выведите $N$ чисел — значения префикс-функции для каждой позиции, разделённые пробелом.

## Пример

Ввод

 ```text
abracadabra
```

Вывод

 ```text
0 0 0 1 0 1 0 1 2 3 4
```
